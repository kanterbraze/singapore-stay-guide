# Example .env.local file
# Copy this to .env.local and fill in your actual values

# Google Maps API Key (client-side, must have HTTP referrer restrictions)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Gemini API Key (server-side only, used in Cloud Function)
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini Proxy URL (set this after deploying the Cloud Function)
# Leave empty for local development with direct API calls
# VITE_GEMINI_PROXY_URL=https://your-region-your-project.cloudfunctions.net/geminiProxy
